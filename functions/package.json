{
  "name": "palengkego-functions",
  "version": "0.1.0",
  "private": true,
  "description": "PalengkeGo trusted backend: orders, reviews, sales rollup, reports.",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --project demo-palengkego",
    "test:rules": "npx firebase emulators:exec --project demo-palengkego --only firestore,auth \"npx jest --runInBand\"",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.0",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.30.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.0"
  }
}
